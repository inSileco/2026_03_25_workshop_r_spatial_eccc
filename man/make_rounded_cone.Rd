% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounded_cone.R
\name{make_rounded_cone}
\alias{make_rounded_cone}
\title{Create a rounded cone polygon from a point}
\usage{
make_rounded_cone(
  pt,
  range_m,
  bearing_deg,
  opening_deg,
  n_side = 40,
  n_cap = 40,
  side_power = 0.7
)
}
\arguments{
\item{pt}{A single point as an \code{sf} or \code{sfc_POINT} object.}

\item{range_m}{Cone length in map units.}

\item{bearing_deg}{Bearing in degrees.}

\item{opening_deg}{Opening angle in degrees.}

\item{n_side}{Number of points used to draw each side.}

\item{n_cap}{Number of points used to draw the rounded cap.}

\item{side_power}{Controls how quickly the cone widens.}
}
\value{
An \code{sfc_POLYGON} object.
}
\description{
Builds a cone-shaped polygon from a single point using a range, a bearing,
and an opening angle. The cone has curved sides and a rounded cap.
}
\examples{
pt <- sf::st_sfc(sf::st_point(c(0, 0)), crs = 32198)

cone <- make_rounded_cone(
  pt = pt,
  range_m = 1000,
  bearing_deg = 45,
  opening_deg = 30
)

plot(cone)
plot(pt, add = TRUE, pch = 16)
}
